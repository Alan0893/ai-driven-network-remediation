# Values override for namespace 
chatbotService:
  replicaCount: 0

ingestionPipeline:
  replicaCount: 0

llama-stack:
  replicaCount: 0

# Mock secrets — replace with real credentials when available
mcpSecrets:
  openshift:
    create: true
    kubeconfig: "placeholder"
  aap:
    create: true
    username: "mock-aap-user"
    password: "mock-aap-password"
  servicenow:
    create: true
    apiKey: "mock-servicenow-key"
